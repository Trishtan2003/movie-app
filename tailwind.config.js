// tailwind.config.js
module.exports = {
  content: [
    './src/**/*.{js,jsx,ts,tsx}', // Make sure to scan the 'src' directory for class names
  ],
  theme: {
    extend: {}, // You can extend the default theme here if needed
  },
  plugins: [],
};
